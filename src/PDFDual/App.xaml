<Application x:Class="PDFDual.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Global Microsoft YaHei Font -->
        <FontFamily x:Key="AppFontFamily">Microsoft YaHei UI, Microsoft YaHei, Segoe UI</FontFamily>

        <!-- Dynamic Theme Brushes with unique Brush prefixes -->
        <SolidColorBrush x:Key="BrushWindowBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BrushCardBg" Color="#282828"/>
        <SolidColorBrush x:Key="BrushCardHoverBg" Color="#303030"/>
        <SolidColorBrush x:Key="BrushCardBorder" Color="#30FFFFFF"/>
        <SolidColorBrush x:Key="BrushTextPrimary" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="BrushTextSecondary" Color="#A8A8A8"/>
        <SolidColorBrush x:Key="BrushInputBg" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="BrushInputBorder" Color="#40FFFFFF"/>
        <SolidColorBrush x:Key="BrushToggleInactiveBg" Color="#323232"/>
        <SolidColorBrush x:Key="BrushToggleInactiveFg" Color="#E2E2E2"/>
        <SolidColorBrush x:Key="BrushDragOverlayBg" Color="#951A1A1A"/>
        <SolidColorBrush x:Key="BrushDragOverlayInner" Color="#250078D4"/>
        <SolidColorBrush x:Key="BrushModalBackdrop" Color="#90000000"/>
        <SolidColorBrush x:Key="BrushScrollThumb" Color="#60FFFFFF"/>
        <SolidColorBrush x:Key="BrushScrollThumbHover" Color="#B0FFFFFF"/>

        <SolidColorBrush x:Key="AccentColor" Color="#0078D4"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#106EBE"/>

        <!-- Fluent Button Style -->
        <Style x:Key="FluentButton" TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource FluentButton}">
            <Setter Property="Background" Value="{DynamicResource BrushToggleInactiveBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource BrushToggleInactiveFg}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource BrushCardBorder}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Microsoft Store Fluent ScrollBar Style -->
        <Style x:Key="FluentScrollBarThumb" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder" 
                                Background="{DynamicResource BrushScrollThumb}" 
                                CornerRadius="2" 
                                Width="3" 
                                HorizontalAlignment="Right" 
                                Margin="0,2,2,2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" Property="Width" Value="8"/>
                                <Setter TargetName="thumbBorder" Property="CornerRadius" Value="4"/>
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource BrushScrollThumbHover}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="thumbBorder" Property="Width" Value="8"/>
                                <Setter TargetName="thumbBorder" Property="CornerRadius" Value="4"/>
                                <Setter TargetName="thumbBorder" Property="Background" Value="{DynamicResource BrushScrollThumbHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource FluentScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>

